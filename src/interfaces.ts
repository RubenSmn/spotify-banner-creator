export interface BannerStyle {
  typography: {
    lineHeight: string;
    color: string;
    fontWeight: string;
  };
  banner: {
    backgroundColor: string;
    justifyContent: string;
    alignItems: string;
  };
}
